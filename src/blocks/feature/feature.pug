mixin feature(params={})
    - const {iconClass='',title='',text=''}=params;
    .feature
        .feature__icon
            i(class=iconClass)
        .feature__text-container
            .feature__title=title
            .feature__text=text
